import { CalendarModule } from '@liff/calendar';
export default CalendarModule;
declare module '@liff/core' {
    interface LiffCore {
        calendar: ReturnType<typeof CalendarModule.prototype.install>;
    }
}
